name: test
channels:
  - conda-forge
  - bioconda
dependencies:
    # Base depends
  - python
  - pip

    # Package depends
  - MDAnalysis
  - gromacs=2019.1
  - pip:
    - GromacsWrapper

    # Testing
  - pytest
  - pytest-cov
  - codecov
