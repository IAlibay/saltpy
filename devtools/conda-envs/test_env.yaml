name: test
channels: conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # Package depends
  - MDAnalysis
  - pip:
    - GromacsWrapper

    # Testing
  - pytest
  - pytest-cov
  - codecov
