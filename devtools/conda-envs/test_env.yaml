name: test
channels:
  - conda-forge
  - bioconda
dependencies:
    # Base depends
  - python
  - pip

    # Package depends
  - MDAnalysis
  - gromacs=2020.3
  - pip:
    - GromacsWrapper

    # Testing
  - pytest
  - pytest-cov
  - codecov
